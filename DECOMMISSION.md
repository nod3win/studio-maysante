# ⚠️ Projet décommissionné — juillet 2026

Ce Sanity Studio n'est **plus utilisé**. Le blog de maysante.be est géré depuis
la plateforme admin (`maysante-admin`) :

- rédaction et publication des articles dans la table MySQL `articles`
  (éditeur TipTap, onglet **Articles**) ;
- génération automatique quotidienne par IA (onglet **Génération IA**), avec
  option de publication automatique.

Le site public (`maysante-next`) ne contient plus aucun code Sanity.

## Finalisation

- Si un déploiement `studio.maysante.be` existe encore, il peut être supprimé
  (ainsi que le sous-domaine DNS).
- Le projet Sanity `czq2nemh` peut être supprimé depuis sanity.io/manage si
  aucun contenu historique ne doit être conservé.

Ce dossier peut ensuite être archivé ou supprimé.
